# Server port and host (Defaults to 8080 and 0.0.0.0)
PORT=8080
HOST=0.0.0.0
NODE_ENV=production

# Client configuration
# Leave empty when served through reverse proxy on same domain
VITE_SERVER_HOST=